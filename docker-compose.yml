version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: webovysystem-app
    volumes:
      - ./:/app
      # Use named volumes for writable runtime directories (avoids Windows bind-mount permission issues)
      - webovysystem_storage:/app/storage
      - webovysystem_bootstrap_cache:/app/bootstrap/cache
    expose:
      - "9000"
    depends_on:
      - mysql

  nginx:
    image: nginx:alpine
    container_name: webovysystem-nginx
    ports:
      - "8000:80"
    volumes:
      - ./:/app
      # Nginx needs to read compiled views and storage-generated files? It mostly proxies to PHP.
      # Keep storage volume consistent in case any paths are referenced.
      - webovysystem_storage:/app/storage
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - app

  mysql:
    image: mysql:8.0
    container_name: webovysystem-mysql
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: laravel
      MYSQL_USER: user
      MYSQL_PASSWORD: user123
    volumes:
      - mysqldata:/var/lib/mysql
    ports:
      - "3306:3306"

  phpmyadmin:
    image: phpmyadmin:latest
    container_name: webovysystem-phpmyadmin
    environment:
      PMA_HOST: mysql
      PMA_USER: root
      PMA_PASSWORD: root
    ports:
      - "8080:80"
    depends_on:
      - mysql

volumes:
  mysqldata:
  webovysystem_storage:
  webovysystem_bootstrap_cache:
